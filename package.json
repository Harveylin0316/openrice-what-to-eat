{
  "name": "openrice-what-to-eat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "find frontend backend netlify/functions -name '*.js' -not -path '*/vendor/*' -not -path '*/node_modules/*' -print0 | xargs -0 -n1 -I{} sh -c 'node --input-type=module --check < \"$1\" || { echo \"SYNTAX ERROR in $1\" >&2; exit 1; }' _ {}"
  },
  "engines": {
    "node": ">=20"
  }
}
